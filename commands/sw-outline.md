---
description: ③ 大纲/节拍表 — 按所选时长的结构蓝图铺节拍
model: opus
---

用 `screenwriter` 技能的 **③ 大纲/节拍表** 阶段处理。产出:节拍表(随时长选变体)+ 一段式梗概。
节拍数/场数必须匹配所选时长蓝图。参考 `skills/screenwriter/references/story_structure.md` + `duration_blueprints.md` 与 `templates/beat_sheet_template.md`。
这是关键确认点:产出后**停下让用户确认方向**再进入分场/成稿。
